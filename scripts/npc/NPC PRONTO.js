var status = 0;
var selection;
var mapleLeaf = 4001126;
var maxCanjes = 5;
var questID = 99998;


function start() {
    status = -1;
    action(1, 0, 0);
}

function action(mode, type, sel) {
        cm.sendOk("Volvera con nuevos eventos!");
        cm.dispose();
        return;
}
